<!doctype html>
<html lang="ko">
<head>
    <meta charset="utf-8"> <!-- 인코딩 -->
    <meta name="viewport" content="width=device-width, initial-scale=1"> <!-- 반응형 -->
    <title>BEMS - 모니터링</title> <!-- 타이틀 -->
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Nanum+Gothic:wght@400;700;800&display=swap"> <!-- 나눔고딕 -->

    <style>
        :root{
          --g:16px;                 /* ✅ 공통 간격(가터) */
          --title-gap:25px;         /* ✅ 상단바↔제목, 제목↔히어로 '같은 간격' */
          --title-h:32px;           /* ✅ 제목 높이(한 줄 고정) = 32px */
          --tile-bg:#09131A;        /* 카드/타일 배경 (하단 버튼과 동일 톤) */
          --tile-border:#1D282F;    /* 카드/타일 스트로크 1px */
          --text:#FAFAFA;           /* 텍스트 색 */
          --header-h:50px;          /* 프레임 상단바 높이 */
          --footer-h:60px;          /* 프레임 하단바 높이 */
          --bottom-cards-h:150px;   /* 하단 3박스 높이 */
        }

        body{ background:#040B12; color:var(--text); } /* 페이지 배경/글자색 */

        /* 큰 화면에서 스크롤 없도록: main에 별도 min-height/padding-bottom 안 줌 (프레임의 padding-top/bottom만 사용) */
        main.page{ /* 비워둠으로써 calc 기반 히어로가 화면을 꽉 채움 */ }

        /* ===== 제목: 상단바와 히어로 사이 간격 '동일' (x:18 고정) ===== */
        main.page > .page-title{
          font-family:"Nanum Gothic","나눔고딕",sans-serif; /* 나눔고딕 */
          font-weight:800;                 /* ExtraBold */
          font-size:32px;                  /* 32px */
          line-height:32px;                /* ✅ 높이를 32px로 고정(계산 편의) */
          margin-top:var(--title-gap);     /* ✅ 상단바(50) 아래로 25px */
          margin-bottom:var(--title-gap);  /* ✅ 히어로 위로도 25px → 위/아래 동일 간격 */
          margin-left:18px;                /* ✅ x=18 */
          margin-right:0;                  /* 우측은 자유 */
          letter-spacing:0;                /* 자간 고정 */
        }

        /* ===== 상단 큰 라운드 박스(히어로) =====
           - 좌우 16px
           - 아래(하단 3박스의 top)와 16px 간격 유지
           - 높이: '보이는 남는 공간'을 꽉 채우도록 calc
              = 100vh
                - 헤더(50)
                - 푸터(60)
                - 하단3박스(150)
                - 하단3박스와의 간격 16
                - 제목 위/아래 간격 25*2
                - 제목 높이 32
        */
        .hero-card{
          margin:0 var(--g) var(--g);                           /* ✅ 좌우 16px, 아래 16px */
          border-radius:14px;                                   /* 모서리 라운드 */
          background:var(--tile-bg);                            /* 배경 */
          border:1px solid var(--tile-border);                  /* 1px 스트로크 */
          min-height:calc(100vh - var(--header-h) - var(--footer-h) - var(--bottom-cards-h)
                          - var(--g) - (var(--title-gap) * 2) - var(--title-h)); /* ✅ 큰 화면에서 스크롤 없이 딱 맞춤 */
          display:flex; align-items:center; justify-content:center; /* 임시 중앙 정렬(컨텐츠 자리) */
        }

        /* ===== 하단 3박스(푸터 위 16px 고정) ===== */
        .bottom-cards{
          position:fixed;                                       /* ✅ 항상 화면 하단 고정 */
          left:var(--g); right:var(--g);                        /* ✅ 좌우 16px */
          bottom:calc(var(--footer-h) + var(--g));              /* ✅ 푸터(60) 위로 16px */
          display:grid; grid-template-columns:repeat(3, 1fr);   /* 3등분 */
          gap:var(--g);                                         /* ✅ 카드 간 16px */
          z-index:1500;                                         /* 푸터(2000) 아래, 본문 위 */
          pointer-events:none;                                  /* 고정층이 클릭 막지 않게 */
        }
        .bottom-card{
          height:var(--bottom-cards-h);                         /* ✅ 150px */
          border-radius:14px;
          background:var(--tile-bg);
          border:1px solid var(--tile-border);
          display:flex; align-items:center; justify-content:center;
          pointer-events:auto;                                  /* 카드 자체는 클릭 가능 */
        }

        /* (옵션) 좁은 화면 레이아웃 전환
        @media (max-width:1400px){ .bottom-cards{ grid-template-columns:repeat(2,1fr);} }  */
        @media (max-width:900px){  .bottom-cards{ grid-template-columns:1fr; } }

    </style>
</head>

<body data-page="monitoring"> <!-- ✅ 이 값으로 하단 '모니터링' 버튼 녹색박스 상시 ON(CSS 매핑) -->


<main class="page">
    <h2 class="page-title">실시간 모니터링</h2>  <!-- ✅ 상단바와 히어로 사이 동일 간격 -->

    <section class="hero-card" aria-label="상단 요약 패널">
        상단 큰 박스(콘텐츠 자리) <!-- 여기에 실시간 지표/미니 그래프/뷰어 등 배치 -->
    </section>
</main>

<!-- ✅ 하단 3박스: 좌우/상하 간격 모두 16px, 푸터 위 고정 -->
<section class="bottom-cards" aria-label="하단 요약 패널">
    <div class="bottom-card">박스 A</div>
    <div class="bottom-card">박스 B</div>
    <div class="bottom-card">박스 C</div>
</section>

</body>
</html>
